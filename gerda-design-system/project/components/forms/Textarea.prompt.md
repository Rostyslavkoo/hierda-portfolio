Multi-line editorial field, sibling of Input; same baseline-rule treatment for messages and enquiries.

```jsx
<Textarea label="Message" rows={5} placeholder="Tell me about the project" />
```

Props: `label`, `rows`, plus all native `<textarea>` attributes.
