Minimal underline text field — no box, a baseline rule that lights to rose on focus, with a tracked uppercase label.

```jsx
<Input label="Name" placeholder="Your name" />
<Input label="Email" type="email" placeholder="you@studio.com" />
```

Props: `label`, plus all native `<input>` attributes.
