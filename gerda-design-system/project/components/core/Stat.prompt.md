Single editorial statistic — large serif value over a tracked uppercase label; use for model stats (height, bust/waist/hips, location).

```jsx
<Stat value="178 cm" label="Height" />
<Stat value="Madrid" label="Based in" align="center" />
```

Props: `value`, `label`, `align` ('left' | 'center').
